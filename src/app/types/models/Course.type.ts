import { Category } from "./Category.type"
import { Student } from "./Student.type"

export type Course= {
    id: number,
    name: string,
    index: string,
    createBy: string,
    createdAt:Date ,
    updateBy: string,
    updatedAt: Date,
   
}